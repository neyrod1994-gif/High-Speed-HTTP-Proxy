name: HTTP Injector Server

on:
  workflow_dispatch:

jobs:
  http-injector:
    runs-on: windows-latest
    timeout-minutes: 15

    steps:
    - name: Install Tailscale
      run: |
        Write-Host "🔧 Instalando Tailscale..."
        $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi"
        $installerPath = "$env:TEMP\tailscale.msi"
        Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
        Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait
        & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-injector-$env:GITHUB_RUN_ID
        Start-Sleep -Seconds 10
        $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
        echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV
        Write-Host "✅ Tailscale IP: $tsIP"

    - name: Create HTTP Injector Server
      run: |
        Write-Host "🛠️ Creando servidor HTTP Injector..."
        
        # Crear directorio de trabajo
        $serverDir = "C:\http-injector"
        New-Item -ItemType Directory -Path $serverDir -Force
        
        # Crear script Python CORREGIDO
        $pythonScript = @'
import http.server
import socketserver
import threading
import time
from datetime import datetime

class InjectorHTTPHandler(http.server.SimpleHTTPRequestHandler):
    
    def do_GET(self):
        print(f"Solicitud recibida: {self.path}")
        
        if self.path == '/payload.txt':
            self.serve_payload()
        elif self.path == '/config.ini':
            self.serve_config()
        elif self.path == '/status':
            self.serve_status()
        else:
            self.serve_index()
    
    def serve_payload(self):
        payload = """# 🚀 HTTP Injector Payload - GitHub Actions
# Server: localhost

[CONNECTIONS]
CONNECT [host_port] [protocol][split][ua] HTTP/1.1

[HEADERS]
Host: mhost.com[crlf]X-Online-Host: mhost.com[crlf]X-Forwarded-Host: mhost.com[crlf]Connection: Keep-Alive[crlf][crlf]

[REQUESTS]
GET http://mhost.com/ [protocol][crlf]Host: mhost.com[crlf]X-Online-Host: mhost.com[crlf]X-Forwarded-Host: mhost.com[crlf]Connection: Keep-Alive[crlf][crlf]

[PORTS]
80,8080,443,53

[PROTOCOLS]
http://

[METHODS]
CONNECT, GET, POST
"""
        self.send_response(200)
        self.send_header('Content-type', 'text/plain')
        self.send_header('Access-Control-Allow-Origin', '*')
        self.end_headers()
        self.wfile.write(payload.encode('utf-8'))
    
    def serve_config(self):
        config = """[SERVER]
host = localhost
port = 8080

[PAYLOAD]
url = http://localhost:8080/payload.txt
update_interval = 3600
"""
        self.send_response(200)
        self.send_header('Content-type', 'text/plain')
        self.send_header('Access-Control-Allow-Origin', '*')
        self.end_headers()
        self.wfile.write(config.encode('utf-8'))
    
    def serve_status(self):
        status = f"HTTP Injector Server - Online\nTime: {datetime.now()}\nStatus: ✅ ACTIVE"
        self.send_response(200)
        self.send_header('Content-type', 'text/plain')
        self.send_header('Access-Control-Allow-Origin', '*')
        self.end_headers()
        self.wfile.write(status.encode('utf-8'))
    
    def serve_index(self):
        html = """<!DOCTYPE html>
<html>
<head>
    <title>🚀 HTTP Injector Server</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; background: #0d1117; color: #c9d1d9; }
        .container { max-width: 800px; margin: 0 auto; }
        .card { background: #161b22; padding: 20px; margin: 10px 0; border-radius: 8px; border: 1px solid #30363d; }
        .btn { display: inline-block; padding: 10px 20px; margin: 5px; background: #238636; color: white; text-decoration: none; border-radius: 6px; }
    </style>
</head>
<body>
    <div class="container">
        <h1>🚀 HTTP Injector Server</h1>
        <div class="card">
            <h3>📥 Download Resources</h3>
            <a class="btn" href="/payload.txt">📄 Payload File</a>
            <a class="btn" href="/config.ini">⚙️ Config File</a>
            <a class="btn" href="/status">📊 Server Status</a>
        </div>
        <div class="card">
            <h3>📱 How to Use in HTTP Injector</h3>
            <p><strong>Payload URL:</strong> http://IP_TAILSCALE:8080/payload.txt</p>
            <p>Replace IP_TAILSCALE with your actual Tailscale IP</p>
        </div>
    </div>
</body>
</html>"""
        self.send_response(200)
        self.send_header('Content-type', 'text/html')
        self.send_header('Access-Control-Allow-Origin', '*')
        self.end_headers()
        self.wfile.write(html.encode('utf-8'))
    
    def log_message(self, format, *args):
        print(f"[{datetime.now()}] {format % args}")

def run_server():
    PORT = 8080
    with socketserver.TCPServer(("", PORT), InjectorHTTPHandler) as httpd:
        print(f"🚀 HTTP Injector Server running on port {PORT}")
        print("📡 Serving payloads at:")
        print("   http://localhost:8080/payload.txt")
        print("   http://localhost:8080/config.ini")
        print("   http://localhost:8080/")
        httpd.serve_forever()

if __name__ == "__main__":
    run_server()
'@

        # Guardar script del servidor
        $pythonScript | Out-File -FilePath "$serverDir\server.py" -Encoding UTF8
        Write-Host "✅ Servidor HTTP Injector creado en $serverDir"

    - name: Install Python
      run: |
        Write-Host "🐍 Verificando Python..."
        # Verificar si Python está instalado
        if (Get-Command python -ErrorAction SilentlyContinue) {
            Write-Host "✅ Python ya está instalado"
        } else {
            Write-Host "📥 Instalando Python..."
            # Instalar Python portable
            $pythonUrl = "https://www.python.org/ftp/python/3.11.0/python-3.11.0-embed-amd64.zip"
            $pythonZip = "$env:TEMP\python.zip"
            $pythonDir = "C:\Python311"
            
            Invoke-WebRequest -Uri $pythonUrl -OutFile $pythonZip
            Expand-Archive -Path $pythonZip -DestinationPath $pythonDir -Force
            $env:Path = "$pythonDir;" + $env:Path
            [Environment]::SetEnvironmentVariable("Path", $env:Path, "Process")
        }
        
        # Verificar Python
        python --version

    - name: Configure Firewall
      run: |
        Write-Host "🔥 Configurando firewall..."
        # Abrir puerto para el servidor HTTP
        netsh advfirewall firewall add rule name="HTTP Injector" dir=in action=allow protocol=TCP localport=8080
        Write-Host "✅ Puerto 8080 abierto en el firewall"

    - name: Start HTTP Server
      run: |
        Write-Host "🚀 Iniciando servidor HTTP Injector..."
        $serverDir = "C:\http-injector"
        
        # Cambiar al directorio del servidor
        Set-Location $serverDir
        
        # Iniciar servidor en segundo plano
        $serverJob = Start-Job -ScriptBlock {
            Set-Location "C:\http-injector"
            python server.py
        }
        
        # Guardar ID del job
        echo "SERVER_JOB_ID=$($serverJob.Id)" >> $env:GITHUB_ENV
        
        Write-Host "⏳ Esperando que el servidor inicie..."
        Start-Sleep -Seconds 5
        
        # Verificar que está funcionando
        $httpStatus = netstat -an | Select-String ":8080"
        Write-Host "🔍 Estado del servidor (8080): $(if($httpStatus) {'✅ ACTIVO'} else {'❌ INACTIVO'})"
        
        if ($httpStatus) {
            Write-Host "🎉 Servidor iniciado correctamente!"
        } else {
            Write-Host "❌ Error: Servidor no inició"
        }

    - name: Show Connection Info
      run: |
        Write-Host ""
        Write-Host "🎉 ===== HTTP INJECTOR SERVER LISTO ====="
        Write-Host "📍 IP del Servidor: $env:TAILSCALE_IP"
        Write-Host ""
        Write-Host "🔗 URLs IMPORTANTES:"
        Write-Host "   📄 Payload URL: http://$env:TAILSCALE_IP:8080/payload.txt"
        Write-Host "   ⚙️ Config URL: http://$env:TAILSCALE_IP:8080/config.ini"
        Write-Host "   🌐 Panel Web: http://$env:TAILSCALE_IP:8080/"
        Write-Host ""
        Write-Host "📱 Para HTTP Injector:"
        Write-Host "   1. Abre HTTP Injector"
        Write-Host "   2. Añade nueva configuración"
        Write-Host "   3. Usa esta URL: http://$env:TAILSCALE_IP:8080/payload.txt"
        Write-Host "   4. Conecta y disfruta! 🎉"
        Write-Host ""
        Write-Host "🚀 Velocidad esperada: 200-500 Mbps"
        Write-Host "⏰ Tiempo activo: 10 minutos"
        Write-Host "======================================"

    - name: Maintain Server
      run: |
        Write-Host "🔄 Manteniendo servidor activo..."
        $startTime = Get-Date
        $endTime = $startTime.AddMinutes(10)
        
        while ((Get-Date) -lt $endTime) {
            $remaining = $endTime - (Get-Date)
            $minutes = [math]::Round($remaining.TotalMinutes, 1)
            
            # Verificar que el servidor sigue activo
            $httpActive = netstat -an | Select-String ":8080"
            
            Write-Host "[$(Get-Date)] Servidor activo - $minutes minutos restantes - Estado: $(if($httpActive){'✅ ACTIVO'}else{'❌ INACTIVO'})"
            
            # Reiniciar si se cayó
            if (-not $httpActive) {
                Write-Host "🔄 Reiniciando servidor..."
                Get-Job -Id $env:SERVER_JOB_ID -ErrorAction SilentlyContinue | Remove-Job -Force
                Start-Sleep -Seconds 2
                Set-Location "C:\http-injector"
                Start-Job -ScriptBlock {
                    Set-Location "C:\http-injector"
                    python server.py
                }
            }
            
            Start-Sleep -Seconds 30
        }
        
        Write-Host "🛑 Apagando servidor HTTP Injector..."
        Get-Job -ErrorAction SilentlyContinue | Remove-Job -Force
